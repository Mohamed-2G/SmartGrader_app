Flask==3.0.0              # Main Flask web framework
Flask-Login==0.6.3        # User session & login management
Flask-Mail==0.10.0        # Sending emails from Flask
Flask-Migrate==4.1.0      # Database migrations with Alembic
Flask-SQLAlchemy==3.1.1   # SQLAlchemy integration with Flask
Werkzeug==3.0.1           # WSGI utilities (used internally by Flask)
blinker==1.9.0            # Signals support for Flask

# Database and ORM 
SQLAlchemy==2.0.42        # Core database ORM
greenlet==3.2.3           # Concurrency support required by SQLAlchemy
alembic==1.16.4           # Database migration tool
Mako==1.3.10              # Templates for Alembic migrations

# Environment and Config
python-dotenv==1.0.0      # Load environment variables from .env
click==8.2.1              # CLI commands (used by Flask)
colorama==0.4.6           # Cross-platform terminal colors (Windows support)

# HTTP Requests 
requests==2.31.0          # Standard HTTP requests
httpx==0.28.1             # Async HTTP client (sometimes required)
httpcore==1.0.9           # Core networking layer for httpx
urllib3==2.5.0            # HTTP library used by requests
idna==3.10                # Unicode domain support
certifi==2025.8.3         # SSL certificates
charset-normalizer==3.4.2 # Encoding detection
sniffio==1.3.1            # Async compatibility helper

#  OpenAI & API Clients
openai==1.3.0             # OpenAI Python SDK (for LLMs / DeepSeek-like APIs)
PyYAML==6.0.2             # YAML file parsing (config files)
pydantic==2.11.7          # Data validation for API responses
pydantic_core==2.33.2     # Core engine for Pydantic
typing_extensions==4.14.1 # Extra typing utilities
typing-inspection==0.4.1  # Tools for inspecting type hints
regex==2025.7.34          # Advanced regex support

# PDF Processing 
PyMuPDF==1.24.11          # Work with PDF and images
PyPDF2==3.0.1             # PDF reading/writing

# Image Processing 
pillow==11.3.0            # Image processing
pytesseract==0.3.10       # OCR (requires Tesseract installed separately)
opencv-python==4.8.1.78   # Computer vision and image handling

# Data Processing 
numpy==2.2.6              # Scientific computing & arrays

# System Utilities
psutil==7.0.0             # System resource monitoring (CPU, memory, etc.)

# Jinja2 Template Engine (used by Flask)
Jinja2==3.1.6
MarkupSafe==3.0.2

# Build Utilities
setuptools==80.9.0
wheel==0.45.1
